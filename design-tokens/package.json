{
  "name": "design-tokens",
  "version": "1.1.0",
  "description": "Shared Hrifa colour token source (Style Dictionary) plus hand-authored fonts.css and components.css. Builds base.css, light.css, and dark.css; components.css/fonts.css are checked in directly. All consumed by every applet.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "style-dictionary build --config config.js"
  },
  "license": "ISC",
  "devDependencies": {
    "style-dictionary": "^5.5.0"
  }
}
